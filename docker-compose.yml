
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123456
      POSTGRES_DB: tunstool_db
    ports:
      - "5435:5432"

  backend:
    build: ./backend
    container_name: backend
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://admin:admin123456@db:5432/tunstool_db
    ports:
      - "8100:8000"

  frontend:
    build: ./frontend
    container_name: frontend
    restart: always
    ports:
      - "3100:3000"
