# Server-side only — never expose to browser
API_BASE_URL=http://localhost:8100

# Optional: base URL encoded in table QR codes (use LAN IP for mobile testing)
# NEXT_PUBLIC_QR_BASE_URL=http://192.168.1.174:3100
